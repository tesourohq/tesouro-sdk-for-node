/**
 * Jest test setup file
 * 
 * This file runs before all tests and can be used to configure
 * global test utilities, mocks, or environment setup.
 */

// Global test utilities or configurations can be added here
beforeAll(() => {
  // Global setup for all tests
});

afterAll(() => {
  // Global cleanup for all tests
});